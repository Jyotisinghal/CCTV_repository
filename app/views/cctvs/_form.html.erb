<%= form_with(model: cctv, local: true) do |form| %>
  <% if cctv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cctv.errors.count, "error") %> prohibited this cctv from being saved:</h2>

      <ul>
      <% cctv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
